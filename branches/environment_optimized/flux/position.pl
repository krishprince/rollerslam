:- ['flux'].

%% define the static objects position




%% Functions to find out where is the agent

 isOnField(S):-;

 isOnInnerTrack(S):-;

 isOnRamp(S):-;

 isOnEdge(S):-;

 isOnInnerTramp(S):-;

 isOnOuterTramp(S):-;
